/*
import introduction.Maths
import org.scalatest.funsuite.AnyFunSuite

//Red -> Green -> Refactor

class MathsFunSuite extends AnyFunSuite {

  test("factorial(0) is 0") {
    assert(Maths.factorial(0) == 0)
  }

  test("factorial(1) is 1") {
    assert(Maths.factorial(1) == 1)
  }

  test("factorial(2) is 2") {
    assert(Maths.factorial(2) == 2)
  }

  test("factorial(3) is 6") {
    assert(Maths.factorial(3) == 6)
  }

  test("factorial(4) is 24") {
    assert(Maths.factorial(4) == 24)
  }

  test("factorial(-1) is 0") {
    assert(Maths.factorial(-1) == 0)
  }
}

// rational numbers / SICP, Newtons Square root method
// 1/2 2/3: numerator and denominator write a class to represent Rational number
// and add two rational numbers 1/2 + 2/3
// and other math operations on rational number - , * , /
// gcd -> between two divisor
// 2 * 2/3 -> use implicit for defining *

// Code Kata
// Code retreat :

*/
